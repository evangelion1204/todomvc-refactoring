'use strict';
/*global require*/
var assert = require('assert');
//var TodoApp = require('../js/app');

describe('TodoApp ', function() {
  it('is available', function () {
    //assert.notEqual(new TodoApp(), undefined);
    assert.equal(true, true);
  });
});
